# Authored golden fixture

Keep design status separate from runtime status: the unit may remain design-ready while BLOCKED_UPSTREAM by a hard dependency on the previous Select and observed state.

Human review target: useful design can continue without premature generation.
