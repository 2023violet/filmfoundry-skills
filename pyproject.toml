[project]
name = "filmfoundry-skills"
version = "3.0.0"
description = "Creator-first script development and visual-production preparation Skills"
requires-python = ">=3.11"

[project.scripts]
ff = "filmfoundry.cli:main"

[tool.setuptools.packages.find]
include = ["filmfoundry*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
norecursedirs = ["historical"]
