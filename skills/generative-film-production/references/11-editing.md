# Editing

Editing consumes approved Selects only. Use hard cuts by default; use J/L cuts for motivated audio continuity; use transitions only when they express a relationship or hide a necessary seam.

Move deterministic fades, dissolves, wipes, crop/reframe, exact speed changes, assembly, and layout to post when a generator adds no unique value. Preserve edit handles around selected clips when possible.
