# Generic T2V Adapter

Use when no image supplies the opening appearance. Compile subject + environment + composition + lighting, then motion/camera, stages/end states, audio, and locks. Keep a single focal action and avoid decorative keyword stacks that do not change visible output.
