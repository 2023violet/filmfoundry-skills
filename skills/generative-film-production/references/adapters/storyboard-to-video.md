# Storyboard-to-Video Adapter

Use a readable whole storyboard when the chosen model can interpret ordered panels; use independent shot pairs when reshootability and exact start/end states matter more. Do not crop or strip panel metadata preemptively unless a test shows the model renders those artifacts into the output.
