# Veo Adapter

Compile from the canonical spec and confirm the exact Veo model/provider surface before relying on reference inputs, duration, audio, or extension features. Keep dialogue verbatim, make camera motion observable, and use hard timing only when an external clock requires it.
